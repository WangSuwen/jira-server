const User = {
    id: Number, // | INT | not null | ID |
    account: String, // | VARCHAR | not null | 账号 |
    password: String, // | VARCHAR | not null | 密码 |
    name: String, // | VARCHAR | not null | 姓名 |
    avatar: String // | VARCHAR |  | 头像 |
};